#!/usr/bin/env python3

import sys

for line in sys.stdin:
  line = line.strip().lower().split()
  seen = []
  for x in line:
    if x.isalnum:
      seen.append(x)

  for y in seen:
    if 
print(len(seen))
